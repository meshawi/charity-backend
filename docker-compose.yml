services:
  app:
    build: .
    container_name: charity-app
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "3000:3000"
    volumes:
      - charity_documents:/app/documents

volumes:
  charity_documents:
